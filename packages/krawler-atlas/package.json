{
  "name": "@kalisio/krawler-atlas",
  "description": "A Krawler based job to download geographical boundary data (Admin Express, OSM, BDPR).",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "keywords": [
    "krawler",
    "kargo",
    "admin-express",
    "bdpr",
    "osm",
    "kalisio"
  ],
  "author": {
    "name": "KALISIO <contact@kalisio.com>",
    "url": "https://github.com/kalisio"
  },
  "contributors": [],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/kalisio/krawler-ekosystem.git",
    "directory": "packages/krawler-atlas"
  },
  "bugs": {
    "url": "https://github.com/kalisio/krawler-ekosystem/issues"
  },
  "homepage": "https://github.com/kalisio/krawler-ekosystem/tree/main/packages/krawler-atlas",
  "scripts": {
    "build:admin-express": "DOCKER_BUILDKIT=1 docker build --build-arg KRAWLER_TAG=${KRAWLER_TAG:-$(node -p \"require('./package.json').krawler.version\")} -t ${KALISIO_DOCKERHUB_URL:-docker.io}/kalisio/krawler-atlas:admin-express-${IMAGE_TAG:-dev} -f dockerfile.admin-express ../..",
    "build:bdpr": "DOCKER_BUILDKIT=1 docker build --build-arg KRAWLER_TAG=${KRAWLER_TAG:-$(node -p \"require('./package.json').krawler.version\")} -t ${KALISIO_DOCKERHUB_URL:-docker.io}/kalisio/krawler-atlas:bdpr-${IMAGE_TAG:-dev} -f dockerfile.bdpr ../..",
    "build:osm-boundaries": "DOCKER_BUILDKIT=1 docker build --build-arg KRAWLER_TAG=${KRAWLER_TAG:-$(node -p \"require('./package.json').krawler.version\")} -t ${KALISIO_DOCKERHUB_URL:-docker.io}/kalisio/krawler-atlas:osm-boundaries-${IMAGE_TAG:-dev} -f dockerfile.osm-boundaries ../..",
    "release": "git tag \"$(node -p \"const p=require('./package.json');p.name+'@'+p.version\")\" # e.g. git tag @kalisio/krawler-atlas@0.0.0",
    "postrelease": "git push origin \"$(node -p \"const p=require('./package.json');p.name+'@'+p.version\")\" # e.g. git push origin @kalisio/krawler-atlas@0.0.0",
    "lint": "standard --fix"
  },
  "krawler": {
    "version": "2.8.2"
  },
  "dependencies": {
    "@turf/area": "catalog:",
    "@turf/center-of-mass": "catalog:",
    "@turf/flatten": "catalog:",
    "glob": "catalog:",
    "lodash": "catalog:"
  },
  "peerDependencies": {
    "@kalisio/krawler": "workspace:*"
  }
}
